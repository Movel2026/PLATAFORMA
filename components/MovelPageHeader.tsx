/**
 * MovelPageHeader — DEPRECATED.
 *
 * El header global Navbar (en app/layout.tsx) ahora cubre todas las páginas
 * con un único diseño consistente en Movel Blue. Este componente se mantiene
 * como wrapper vacío para no romper imports existentes en páginas internas.
 *
 * No agregar nuevos usos. Para nuevos headers, usar el Navbar global.
 */
export default function MovelPageHeader() {
  return null;
}
